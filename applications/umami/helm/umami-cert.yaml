apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: umami-tls
  namespace: default
spec:
  secretName: umami-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - umami.mohamed-uptime.com