apiVersion: apps/v1
kind: Deployment
metadata:
  name: umami
  namespace: umami
  labels:
    app.kubernetes.io/name: umami
    app.kubernetes.io/instance: umami-release
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: umami
      app.kubernetes.io/instance: umami-release
  template:
    metadata:
      labels:
        app.kubernetes.io/name: umami
        app.kubernetes.io/instance: umami-release
    spec:
      initContainers:
        - name: migrate
          image: ghcr.io/mohamed67471/umami:latest
          command: ["npx", "prisma", "migrate", "deploy", "--schema", "./prisma/schema.prisma"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: umami-db-secret
                  key: DATABASE_URL
      containers:
        - name: umami
<<<<<<< HEAD
          image: ghcr.io/mohamed67471/umami:95342a3
=======
          image: ghcr.io/mohamed67471/umami:latest
>>>>>>> 127e84b (feat: automate db migrations via init container)
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: umami-db-secret
                  key: DATABASE_URL
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: umami-app-secret
                  key: APP_SECRET
            - name: HASH_SALT
              valueFrom:
                secretKeyRef:
                  name: umami-app-secret
                  key: HASH_SALT
            - name: DATABASE_TYPE
              value: "postgresql"
            - name: NODE_EXTRA_CA_CERTS
              value: /etc/ssl/rds-ca/rds-ca.pem
          volumeMounts:
            - name: rds-ca
              mountPath: /etc/ssl/rds-ca
              readOnly: true
          livenessProbe:
            httpGet:
              path: /api/heartbeat
              port: 3000
            initialDelaySeconds: 180
            periodSeconds: 45
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/heartbeat
              port: 3000
            initialDelaySeconds: 150
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: rds-ca
          configMap:
            name: rds-ca